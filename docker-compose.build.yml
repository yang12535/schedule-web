version: '3.8'

# ========================================
# 班级课表服务 - 本地构建配置
# ========================================
# 使用方式: docker-compose -f docker-compose.yml -f docker-compose.build.yml up -d --build
# ========================================

services:
  schedule:
    build: .
    image: schedule-web:local
