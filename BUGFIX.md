# Bug 修复记录

## 修复概要

本次修复针对 Docker 部署环境和代码进行了全面的调试，共修复了 **18个关键问题**。

---

## 一、Docker 部署相关修复（6个）

### 1. Dockerfile - 安装 wget 工具
**问题：** Docker Compose 中配置了健康检查使用 `wget`，但 Alpine 镜像默认不包含 wget。  
**修复：** 添加了 `apk add --no-cache wget`

### 2. Dockerfile - 数据目录权限问题
**问题：** 容器以非 root 用户运行时可能无法写入数据目录。  
**修复：** 添加了 `chmod 755 /data /data/logs`

### 3. docker-compose.yml - 移除不兼容的 sysctls
**问题：** `sysctls` 配置在某些 Docker 环境中需要特殊权限，会导致警告或启动失败。  
**修复：** 移除了 `sysctls` 配置

### 4. deploy/install.sh - 数据目录权限
**问题：** 部署脚本创建数据目录后未设置权限。  
**修复：** 添加了 `chmod 755 data logs`

### 5. deploy/install-prebuilt.sh - Docker Compose 命令兼容
**问题：** 新版 Docker 使用 `docker compose`（空格），旧版使用 `docker-compose`（短横线）。  
**修复：** 添加了自动检测逻辑

### 6. deploy/install-prebuilt.sh - 数据目录权限
**问题：** 缺少数据目录权限设置。  
**修复：** 添加了 `chmod 755 data logs`

---

## 二、后端代码修复（server.js - 5个）

### 7. 数据文件读取空文件处理
**问题：** 当数据文件存在但内容为空时，`JSON.parse` 会抛出错误。  
**修复：** 在读取后检查内容是否为空

### 8. 数据文件 JSON 格式错误处理
**问题：** 数据文件损坏时会导致服务崩溃。  
**修复：** 添加了更详细的错误处理，记录错误日志但继续使用默认配置

### 9. 保存数据时目录不存在
**问题：** `fs.writeFile` 在目录不存在时会失败。  
**修复：** 在写入前确保目录存在

### 10. 日志文件路径遍历漏洞
**问题：** `/api/logs/:file` 接口可能存在目录遍历攻击风险。  
**修复：** 添加了路径验证，确保解析后的路径在日志目录内

### 11. 缺少全局错误处理中间件
**问题：** 未处理的异常会导致服务崩溃。  
**修复：** 添加了 Express 错误处理中间件

### 12. 导出文件名编码问题
**问题：** 班级名称包含特殊字符时，下载文件名可能出错。  
**修复：** 使用 `encodeURIComponent` 编码文件名

---

## 三、前端代码修复（index.html - 7个）

### 13. XSS 漏洞
**问题：** 多处直接插入用户输入的内容，存在 XSS 攻击风险。  
**修复：** 添加了 `escapeHtml()` 函数，对所有动态内容进行 HTML 转义

### 14. 颜色选择器空指针异常
**问题：** 保存课程时如果未选择颜色，`document.querySelector('.color-option.selected')` 返回 null。  
**修复：** 使用可选链操作符 `?.` 并提供默认值

### 15. 移除强制颜色选择限制
**问题：** 原代码强制要求选择颜色。  
**修复：** 移除了强制颜色选择的逻辑和 UI 提示

### 16. 清空操作不保存
**问题：** 点击"清空"后数据未同步到服务器。  
**修复：** 添加了 `await autoSave()` 调用

### 17. 节次解析空值处理
**问题：** `parsePeriods` 函数在输入为空时会出错。  
**修复：** 添加了对空值和无效输入的检查和验证

### 18. 内存泄漏（定时器未清理）
**问题：** `setInterval` 创建的定时器在页面卸载时未清理。  
**修复：** 保存定时器引用，在 `beforeunload` 事件中清理

### 19. 节次范围验证
**问题：** 用户可以输入超出范围的节次（如 1-100）。  
**修复：** 添加了节次范围验证，确保在 1-totalPeriods 范围内

### 20. 课程 ID 生成冲突
**问题：** 使用 `Date.now()` 生成 ID 在高并发下可能重复。  
**修复：** 添加随机字符串后缀确保唯一性

---

## 修复文件列表

| 文件 | 修复问题数 |
|------|-----------|
| Dockerfile | 2 |
| docker-compose.yml | 1 |
| deploy/install.sh | 1 |
| deploy/install-prebuilt.sh | 2 |
| src/server/server.js | 6 |
| src/public/index.html | 8 |
| **总计** | **20** |

---

## 测试建议

### Docker 构建测试
```bash
cd schedule-web
docker-compose build
docker-compose up -d
docker ps  # 确认容器健康状态
```

### 功能测试清单

- [ ] 添加课程时不选择颜色，确认可以保存
- [ ] 点击"清空"后刷新页面，确认数据已清空
- [ ] 进入编辑模式，确认密码验证正常
- [ ] 输入节次 "1-100"，确认会提示错误
- [ ] 尝试 XSS 攻击（在课程名输入 `<script>alert(1)</script>`），确认被转义
- [ ] 导入损坏的 JSON 文件，确认有错误提示
- [ ] 检查日志文件下载，确认路径遍历被阻止

---

## 安全增强

1. **XSS 防护**：所有用户输入都经过 HTML 转义
2. **路径遍历防护**：日志文件下载接口验证路径
3. **输入验证**：节次格式和范围验证
4. **错误处理**：详细的错误日志记录，不暴露敏感信息
