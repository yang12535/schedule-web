# 更新日志

## v1.1.1 (2026-06-06) - 雨云持久化权限修复

### Bug 修复
- 修复雨云托管卷覆盖镜像内 `/data` 权限后，`node` 用户无法创建 `/data/logs` 和保存课表的问题。
- 新增容器 entrypoint：以 root 初始化挂载目录并修正所有权，随后通过 `su-exec` 降权为 `node` 用户运行。
- 初始化或存储检查失败时进程直接退出，不再启动一个只能读取、无法保存的服务。
- `/healthz` 现在实际执行临时文件写入和原子重命名，存储异常时返回 HTTP 503。

## v1.1.0 (2026-06-06) - 雨云 RCA 上架适配

### 部署与安全
- 新增独立的 `GET /healthz` 健康检查，响应为 `{"ok":true,"service":"schedule-web"}`，不依赖课表文件。
- 明确 `EDIT_PASSWORD` 三态行为：未设置时随机生成、具体值固定、显式空字符串关闭保护。
- Docker Compose 不再把未设置的密码变量默认为空，镜像固定为 `v1.1.0`。
- 持久化统一为 `/data`，课表和日志均包含在同一个持久化目录中。

### 雨云 RCA
- 新增面向普通用户的雨云 RCA 部署说明。
- 新增可人工照填的应用版本参考 JSON，包含资源、环境变量、卷、服务和选项。
- 补齐最低配置、密码策略、健康检查、备份与恢复说明。

## v1.0.5 (2026-04-28) - 安全修复与兼容性增强版

### 🔒 安全增强
- **CDN 脚本完整性保护**：Tailwind CSS CDN 引入添加 `crossorigin="anonymous"` 和 SRI (`integrity`) 属性，防止 CDN 篡改攻击。
- **密码输入安全优化**：密码输入框由明文 (`type="text"`) 改为隐藏 (`type="password"`)，新增"显示/隐藏"切换按钮，兼顾安全性与可用性。
- **密码存储方式改进**：编辑密码由全局变量迁移至 `sessionStorage`，减少内存泄漏风险，关闭标签页后自动清除。
- **输入转义加固**：公告日期范围、时段设置时间值增加 HTML 转义；公告 ID 在 API URL 中增加 `encodeURIComponent` 编码。

### 🐛 Bug 修复
- **公告删除无响应**：修复公告列表 `innerHTML` 替换后事件丢失导致编辑/删除按钮无响应的问题（改用事件委托绑定）。
- **原生 confirm 兼容性**：删除公告时不再调用浏览器原生 `confirm()`，改用内置确认弹窗，兼容禁用原生对话框的浏览器及 WebView 嵌入环境。
- **公告编辑数据传递**：编辑公告时改为通过 ID 从数据集中查找对象，避免直接序列化对象到 HTML 属性中引发的引号转义问题。
- **加载失败处理**：公告列表加载失败时显示"加载失败"提示，避免空白无反馈。

### ✨ 改进
- **课程类型扩展**：新增 `default`（默认）、`psychology`（心理学）、`math`（数学）、`chemistry`（化学）4 种课程类型样式与颜色选择器支持。

## v1.0.4 (2026-04-17) - 公告与补课功能版

### ✨ 新功能
- **弹窗公告**：支持时段内生效的公告弹窗，多公告轮播，localStorage 防重复
- **单周补课**：课程编辑新增"补课模式"，仅当前周生效，自动标记补课标签

### 🐛 修复
- 公告日期比较改用本地时间，避免 UTC 时区偏移
- 补课周次限制在合法范围 `[1, totalWeeks]` 内

## v1.0.3 (2026-04-17) - 代码审查修复版

### 🐛 Bug 修复（9项）

#### 后端
- **数据持久化原子写入**：`saveSchedule` 改为临时文件 + `rename` 覆盖，防止进程崩溃导致数据文件半写入损坏
- **API 404 规范化**：未匹配的 `/api/*` 路由统一返回 JSON 404，避免被 SPA fallback 拦截返回 HTML
- **临时文件清理**：`rename` 失败时自动 `unlink` 清理残留 `.tmp.*` 文件

#### 前端
- **跨周日期计算修复**：`findNextCourse` 对 `dayDiff < 0` 补 `+7` 天，避免周五后查找下周一课程时错误回退到本周一
- **周日归一化**：`getDay() === 0` 时视为 7，确保周日时周一～周五的 `weekOffset` 正确设置为 1
- **周次边界限制**：`changeWeek` 增加 `[1, totalWeeks]` 边界检查，防止越界
- **清空后 UI 同步**：`clearAll` 后同步调用 `updateNextCourse()`，底部固定卡片即时刷新
- **缺失样式补充**：补充 `.ending-soon` 样式与 `@keyframes pulse-warning` 脉冲动效
- **时区边缘修复**：`getCurrentWeek` 改用 `Date.UTC` 日粒度计算，避免本地时区/夏令时导致周次偏差

## v1.0.2 (2024-03-24) - 稳定性修复版

### 🐛 Bug 修复（20项重大修复）

#### Docker 部署修复
- 修复 Dockerfile 缺少 wget 工具导致健康检查失败的问题
- 修复数据目录权限问题，确保容器可以正常写入数据
- 移除 docker-compose.yml 中不兼容的 sysctls 配置
- 修复部署脚本中数据目录权限设置问题
- 修复部署脚本 Docker Compose 命令兼容性问题（支持新版 `docker compose` 和旧版 `docker-compose`）

#### 后端安全与稳定性修复
- 修复数据文件为空时的 JSON 解析错误
- 修复数据文件损坏时的服务崩溃问题，添加详细错误处理
- 修复保存数据时目录不存在的问题
- 修复日志文件路径遍历漏洞（安全加固）
- 添加 Express 全局错误处理中间件，防止未处理异常导致崩溃
- 修复导出文件名编码问题，支持特殊字符

#### 前端安全与体验修复
- **修复 XSS 漏洞**：添加 HTML 转义函数，防止脚本注入攻击
- 修复颜色选择器空指针异常
- 移除强制颜色选择限制，颜色选择变为可选
- 修复清空操作不保存到服务器的问题
- 修复节次解析空值处理问题，添加输入验证
- 修复内存泄漏问题，页面卸载时清理定时器
- 添加节次范围验证（确保在 1-totalPeriods 范围内）
- 修复课程 ID 生成冲突问题，添加随机后缀确保唯一性

### 🔒 安全增强
- 全面的 XSS 防护：所有用户输入都经过 HTML 转义
- 路径遍历防护：日志文件下载接口验证路径
- 输入验证：节次格式和范围验证
- 错误处理：详细的错误日志记录，不暴露敏感信息

## v1.0.1 (2024-03-14)

### 🐛 修复
- 修复周末错误标记当前课程的问题（day 6/0 时不高亮）
- 修复下一节课卡片遮挡弹窗底部按钮的问题
- 添加颜色选择强制校验和提示

### ✨ 改进
- 重构下一节课卡片 UI：
  - 上课中（>10分钟）：显示当前课程，绿色边框
  - 即将下课（≤10分钟）：主从切换，大字显示下一节课
  - 课间休息：显示下一节课倒计时，红色边框
- 添加显眼的颜色选择提示（保存前必须选择颜色）

## v1.0.0 (2024-03-12)

### ✨ 新功能
- 课程自动保存（添加/删除后自动同步到服务器）
- 兼容旧版本课表数据导入（自动迁移字段）
- VPS 一键部署脚本（支持 Debian/Ubuntu/CentOS/RHEL）
- Makefile 快捷命令管理

### 🐛 修复
- 空密码环境变量处理（EDIT_PASSWORD="" 表示无需密码）
- Docker 静态文件路径修复

### 📁 目录结构
```
schedule-web/
├── src/server/        # 后端代码
├── src/public/        # 前端代码
├── deploy/            # 部署脚本
├── docker-compose.yml
├── Dockerfile
└── Makefile
```

### 🚀 部署方式
```bash
# VPS 一键部署
curl -fsSL https://raw.githubusercontent.com/yang12535/schedule-web/main/deploy/install.sh | bash

# Docker Compose
cp .env.example .env
make start
```
